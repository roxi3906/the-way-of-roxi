interface:
  display_name: "Roxi's Way"
  short_description: "Enforce delivery rules and context-aware output language"
  default_prompt: "Use $roxis-way to perform this repository task, inspect each output destination before writing, select the language of every output independently from local conventions, and follow all workspace, implementation, validation, Git, PR, and code comment rules."

policy:
  allow_implicit_invocation: true
