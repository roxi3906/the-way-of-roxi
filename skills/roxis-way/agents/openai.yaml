interface:
  display_name: "Roxi's Way"
  short_description: "Route repository work and preserve authorized task choices"
  default_prompt: "Use $roxis-way for this repository task, apply the route that matches the requested outcome, reuse established task choices, and check destination language, authorization, and verification before the relevant actions."

policy:
  allow_implicit_invocation: true
