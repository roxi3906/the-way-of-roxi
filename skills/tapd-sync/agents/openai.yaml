interface:
  display_name: "TAPD Sync"
  short_description: "Sync every substantive work session with TAPD"
  default_prompt: "Use $tapd-sync to match this work session with TAPD and maintain valuable follow-up work."
policy:
  allow_implicit_invocation: true
