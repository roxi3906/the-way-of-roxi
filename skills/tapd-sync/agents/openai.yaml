interface:
  display_name: "TAPD Sync"
  short_description: "Prompt once, then track after parent binding"
  default_prompt: "Use $tapd-sync to match the first response, prompt once for a parent, pause when unbound, and maintain child work items after binding."
policy:
  allow_implicit_invocation: true
