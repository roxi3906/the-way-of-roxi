interface:
  display_name: "TAPD Sync"
  short_description: "Sync session work with configured TAPD items"
  default_prompt: "Use $tapd-sync to match this session with TAPD work items and keep valuable follow-up work synchronized."
